<!-- Development infrastructure: this template governs pull requests to this repository.
     It is never installed onto a user's estate. -->

Fixes #NN

<!-- REQUIRED — the merge gate reads this body for that line. Replace NN with the number of the
     issue this closes; leave the letters in and the check reds, which is deliberate.
     Write the REAL number: an example number left here closes THAT issue on merge.

     Accepted, and nothing else:  Fixes #NN  |  Closes #NN  |  Resolves #NN
     Any capitalisation, and one or more SPACES before the '#'.

     NOT accepted: Fix, Fixed, Close, Closed, Resolve, Resolved (GitHub honours those, this gate
     does not); a colon, as in "Fixes: #NN"; a tab or a line break in place of the space; and a
     bare "#NN" mention on its own, which is deliberately not enough.

     The number must be an OPEN issue in this repository, not a pull request. To close more than
     one, write a keyword for each.

     Your branch must lead with one of the numbers closed here (the NN-slug grammar), except on a
     fork, where the branch name is informational. -->

## What this changes

<!-- One or two sentences of plain English: what the diff does, and why. -->

## How it was proven

<!-- What you ran and what it said. If this fixes a bug, name the guard that fails on the
     pre-fix code. If you ran nothing, say so. -->
