One folder per ticket, initialised from the template by the `ticket-init` agent.
Nothing here states a rule. Ticket naming, the folder states and the markers are
law, and the law is `folder-structure.md`. The map is `folder-map.md`.
