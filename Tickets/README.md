# Tickets

One folder per ticket, named `YYYYMM[A-Z]-PROJ-XXXXX`, initialised from the
template by the `ticket-init` agent. The human map of the whole harness is
the repository root `README.md`; the law is `folder-structure.md`.
